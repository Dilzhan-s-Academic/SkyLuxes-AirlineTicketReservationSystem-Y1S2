function openPopup(elementID) {
    var viewReservation = document.getElementById(elementID);
    viewReservation.style.visibility = "visible";
}
function closePopup(elementID) {
    var viewReservation = document.getElementById(elementID);
    viewReservation.style.visibility = "hidden";
}